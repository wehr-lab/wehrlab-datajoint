# .readthedocs.yml

version: 2

sphinx:
  configuration: docs/conf.py

formats:
   - pdf

build:
  os: ubuntu-20.04
  tools:
    python: "3.9"

python:
  install:
    - method: pip
      path: ./python
      extra_requirements:
        - docs